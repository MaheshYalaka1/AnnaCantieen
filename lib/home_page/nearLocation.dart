import 'package:flutter/material.dart';

class nearLocation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('data'),
      ),
      body: const Center(
          child: Column(
        children: [Text('Implementing')],
      )),
    );
  }
}
